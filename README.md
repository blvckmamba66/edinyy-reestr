# Единый реестр граждан

Демонстрационный сайт на русском языке: регистрация пользователей по номеру телефона,
сбор персональных данных с согласием на обработку и админ-панель с экспортом в Excel.

## Возможности

- **Регистрация** по форме: Имя, Фамилия, Пол (М/Ж), Место рождения, Гражданство,
  Адрес фактического проживания, Номер телефона.
- **Подтверждение телефона по SMS-коду** (демо-режим из коробки, реальная отправка через SMS.ru).
- **Обязательная галочка** согласия на обработку персональных данных.
- **Вход** по номеру телефона, личный кабинет с просмотром данных.
- **Админ-панель** (`/admin`): просмотр всех записей, поиск, редактирование данных,
  комментарии администратора, удаление пользователей, статистика.
- **Произвольные дополнительные поля** — администратор может добавить любому человеку
  любые сведения (например, «Дети» — «двое», «Образование» — «высшее»).
- **Выгрузка всех данных в Excel** (`.xlsx`) одной кнопкой, включая произвольные поля
  отдельными столбцами.

## Подтверждение по SMS

По умолчанию работает **демо-режим**: SMS не отправляется, а код показывается на экране
регистрации (и в логах сервера) — удобно для разработки и демонстрации.

Чтобы включить **реальную отправку** через [SMS.ru](https://sms.ru):

```powershell
$env:SMSRU_API_ID = "ваш-api-id-из-личного-кабинета-sms.ru"
npm start
```

На Render задайте переменную `SMSRU_API_ID` в разделе **Environment**.
Если переменная не задана — автоматически работает демо-режим.

## Технологии

- Node.js + Express
- Встроенный `node:sqlite` (файл `data.db` создаётся автоматически) — нативных зависимостей нет
- ExcelJS для экспорта
- HTML / CSS / ванильный JavaScript на фронтенде

## Запуск

```powershell
npm install
npm start
```

Затем откройте:

- Сайт: <http://localhost:3000>
- Админ-панель: <http://localhost:3000/admin>

## Доступ администратора

По умолчанию: **логин** `admin`, **пароль** `admin123`.

Измените их через переменные окружения перед запуском:

```powershell
$env:ADMIN_LOGIN = "myadmin"
$env:ADMIN_PASSWORD = "сложный-пароль"
$env:SESSION_SECRET = "случайная-строка"
npm start
```

## Примечания по безопасности

Это учебно-демонстрационный проект. Для реальной эксплуатации необходимо:
подтверждение телефона по SMS-коду, хеширование/защита учётных данных администратора,
HTTPS, хранилище сессий вне памяти процесса и соответствие требованиям 152-ФЗ
«О персональных данных».
